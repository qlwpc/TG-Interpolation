# Slurm validation summary — job 3868

- Partition/node: `normal` / `rtx3090b`
- Allocation: 1 node, 2 billed CPUs, 1 GPU
- Final state: `COMPLETED`
- Exit code: `0:0`
- Elapsed time: 32 seconds
- Environment: `/home/wangpch/.conda/envs/LLM`
- Visible GPU: one NVIDIA GeForce RTX 3090 (`CUDA_VISIBLE_DEVICES=0`)

## Passed checks

- `nvidia-smi`: driver 535.230.02, allocated RTX 3090 visible.
- PyTorch: 2.7.1+cu126; CUDA 12.6 runtime; real CUDA matrix multiplication.
- FlashAttention: 2.8.2 FP16 forward and backward CUDA kernels.
- Python stack: NumPy 2.2.6 and NLTK 3.9.3 operations.
- Native extensions: `tg_mask` and `cppbackend` imports.
- Benepar 0.2.0 parser with protobuf 6.33.6 compatibility setting.
- W&B 0.24.1 offline initialization, logging, and shutdown.
- Spack CMake 3.29.6.
- `python -m pip check`: no broken requirements.
- `conda doctor -n LLM`: healthy; no altered or missing files.
- Relevant repository regression: 17 tests passed.
- CUDA 11 pip runtime packages: absent.

## Interpretation

The direct, non-allocation shell's earlier NVML error was context-specific.
GPU availability must be validated inside a Slurm GPU allocation on this
cluster; job 3868 demonstrates that the current driver, CUDA 12 pip runtime,
PyTorch, and FlashAttention path works.

This job tested the third-party `flash-attn` package at head dimension 64. It
did **not** call `torch.nn.attention.flex_attention`, so it did not cover the
reported FlexAttention short-sequence (`N < 128`) backward path. That gap is
addressed by jobs 3879, 3880, 3885, and 3886; see
`2026-09-01-flex-attention-short-sequence-upgrade-summary.md`. Jobs 3869 and
3874 are retained only as a separate `head_dim=128` control.

`nvcc` is not installed. This does not affect the verified training/runtime
path because FlashAttention is installed from a compatible prebuilt wheel. A
CUDA toolkit is only needed if a CUDA extension must be compiled from source.

Non-failing warnings were limited to the approaching Python 3.10 end of life,
legacy T5 tokenizer behavior, a `torch_struct` constraints warning, and W&B
offline informational output.

## Evidence

- Structured result: `results/tg-llm-env_3868.json`
- Standard output: `tg-llm-env_3868.out`
- Standard error: `tg-llm-env_3868.err`
- Reusable job: `verify_llm_gpu.sbatch`
- Validator: `verify_llm_gpu.py`
